ellipsis() {
	white-space: nowrap;
	text-overflow: ellipsis;
	overflow: hidden;
}

flex-row() {
	display: flex;
	flex-direction: row;
}

flex-column() {
	display: flex;
	flex-direction: column;
}

vh-center() {
	display: flex;
	justify-content: center;
	align-items: center;
}